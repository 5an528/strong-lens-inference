venv/
.venv/
.idea/
__pycache__/
*.pyc
.ipynb_checkpoints/

# ADDED: generated dataset/model files -- large, and fully reproducible by
# running `python main.py generate` / `python main.py train` (see README).
data/processed/
data/models/

# LaTeX build artifacts (report/report.tex and the compiled PDF are tracked)
report/*.aux
report/*.log
report/*.out
report/*.toc
report/*.synctex.gz
report/*.fls
report/*.fdb_latexmk